# LLM Configuration - REQUIRED
OPENAPI_LLM_URL=https://api.openai.com/v1
OPENAPI_LLM_API_KEY=your_openai_api_key_here
OPENAPI_LLM_MODEL=gpt-4

# ChromaDB Configuration for RAG (matches chives-ingestion naming)
CHROMA_COLLECTION_NAME=documents
CHROMA_PERSIST_DIRECTORY=./chroma_db
CHROMA_EMBEDDING_MODEL=all-MiniLM-L6-v2
CHROMA_HOST=localhost
CHROMA_PORT=8000

# MongoDB Configuration (if needed for document metadata)
MONGODB_CONNECTION_STRING=mongodb://localhost:27018
MONGODB_DATABASE_NAME=chives_ingestion
MONGODB_COLLECTION_NAME=documents

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS Configuration
ALLOWED_HOSTS=["http://localhost:3000", "http://127.0.0.1:3000"] 