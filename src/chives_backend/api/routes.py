"""API routes - currently unused.

All agent-related endpoints are defined directly in main.py.
This file is kept for future API extensions if needed.
"""

from fastapi import APIRouter

api_router = APIRouter()

# Currently empty - all endpoints are in main.py
